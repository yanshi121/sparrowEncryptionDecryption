from setuptools import setup, find_packages

setup(
    name='sparrowEncryptionDecryption',
    version='0.1.2',
    description='基于进制转换的浅加密工具',
    packages=find_packages(),
    python_requires='>=3.11',
    install_requires=[]
)

